/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Wed Mar 04 2026 11:02:34 GMT+0530 (India Standard Time)
	Description : Interface Logic for BCSSteam List Details.
	WARNING : DO NOT MODIFY THE GENERATED INTERFACE METHOD NAME OR ARGUMENTS.
	Custom code can be writter here.
	Use customDtOpns object to maniuplate data table.
*/

var alphanumericRegex = /^[a-zA-Z0-9 ]+$/;
var dashRegex = /^[a-zA-Z0-9 -]+$/;

if (typeof TenantSteamFlowMeterPointInterface === 'undefined')
	TenantSteamFlowMeterPointInterface = {};

var SteamFlowMeterPointInterface = {
	onLoad: function() {
		//Custom code goes here
		document.querySelector("title").innerHTML = "BCS Steam";
		document.querySelector("span").innerHTML = "BCS Steam";

		document.querySelector("span[data-string-key='CREATE_STEAMFLOWMETERPOINT']").innerHTML = "Create SFMP";
		document.querySelector("span[data-string-key='UPDATE_STEAMFLOWMETERPOINT']").innerHTML = "Update SFMP";

		document.querySelector("label[data-string-key='LABEL_SFMP_DESCRIPTION']").innerHTML = "SFMP Description*";

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onLoad)
			return TenantSteamFlowMeterPointInterface.onLoad();

		return true;
	},

	onBeforeLoad: function(dataSources) {


		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onBeforeLoad)
			return TenantSteamFlowMeterPointInterface.onBeforeLoad();

		return dataSources;
	},

	onAfterLoad: function(dataSources) {



		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onAfterLoad)
			return TenantSteamFlowMeterPointInterface.onAfterLoad(dataSources);

		return dataSources;
	},

	onBeforeData: function(params, dataSources) {



		//Custom code goes here
		clCommon.hide(filterSteamFlowMeterPointBtn);



		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onBeforeData)
			return TenantSteamFlowMeterPointInterface.onBeforeData(params, dataSources);

		return true;
	},

	onAfterData: function(isSuccessful, dataArr, dataSources) {
		//custom code goes here

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onAfterData)
			return TenantSteamFlowMeterPointInterface.onAfterData(isSuccessful, dataArr, dataSources);

		return dataArr;
	},

	renderData: function(value, type, dtObj, meta) {
		//custom code goes here

		var rVal;

		switch(meta.col){
			case 0:
				rVal = Strings.NA_STRING;
				if(value){
					rVal = value;
				}
			case 1:
				rVal = Strings.NA_STRING;
				if(value){
					rVal = value;
				}

				break;

			case 2:
				rVal = Strings.FALSE_STRING;

				if(dtObj.sfmp_active){
					rVal = Strings.TRUE_STRING;
				}
				break;
		}

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.renderData)
			return TenantSteamFlowMeterPointInterface.renderData(value, type, dtObj, meta);

		return rVal;
	},

	onBeforeCreate: function(dataObj, dataSources) {
		//custom code here
		if (dataObj && dataObj.sfmp_code) {
			var value = dataObj.sfmp_code;

			if (value.length > 20) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_CODE_LENGTH, dlgOptions);
				return false;
			}

			if (!dashRegex.test(value)) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_CODE_FORMAT, dlgOptions);
				return false;
			}
		}

		if (dataObj && dataObj.sfmp_description) {
			var value = dataObj.sfmp_description;

			if (value.length > 50) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_DESC_LENGTH, dlgOptions);
				return false;
			}

			if (!alphanumericRegex.test(value)) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_DESC_FORMAT, dlgOptions);
				return false;
			}
		}


		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onBeforeCreate)
			return TenantSteamFlowMeterPointInterface.onBeforeCreate(dataObj, dataSources);

		return dataObj;
	},

	onAfterCreate: function(isSuccessful, dataObj, dataSources) {
		//Custom code here
		if(dataObj.err_code === 409){
			clCommon.hideLoader("createProgress");
			clCommon.enable(createSteamFlowMeterPointBtn);
			s_info(Strings.ERROR, Strings.DUPLICATE_SFMP, dlgOptions);

			return false;
		}


		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onAfterCreate)
			return TenantSteamFlowMeterPointInterface.onAfterCreate(isSuccessful, dataObj, dataSources);

		return dataObj;
	},

	onBeforeUpdate: function(dataObj, dataSources) {
		//custom code here
		if (dataObj && dataObj.sfmp_code) {
			var value = dataObj.sfmp_code;

			if (value.length > 20) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_CODE_LENGTH, dlgOptions);
				return false;
			}

			if (!dashRegex.test(value)) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_CODE_FORMAT, dlgOptions);
				return false;
			}
		}

		if (dataObj && dataObj.sfmp_description) {
			var value = dataObj.sfmp_description;

			if (value.length > 50) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_DESC_LENGTH, dlgOptions);
				return false;
			}

			if (!alphanumericRegex.test(value)) {
				s_info(Strings.ERROR, Strings.INVALID_SFMP_DESC_FORMAT, dlgOptions);
				return false;
			}
		}

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onBeforeUpdate)
			return TenantSteamFlowMeterPointInterface.onBeforeUpdate(dataObj, dataSources);

		return dataObj;
	},

	onAfterUpdate: function(isSuccessful, dataObj, dataSources) {
		//Custom code here
		if(dataObj.err_code === 409){
			clCommon.hideLoader("updateProgress");
			clCommon.enable(editSteamFlowMeterPointBtn);
			s_info(Strings.ERROR, Strings.DUPLICATE_SFMP, dlgOptions);

			return false;
		}


		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onAfterUpdate)
			return TenantSteamFlowMeterPointInterface.onAfterUpdate(isSuccessful, dataObj, dataSources);

		return dataObj;
	},

	onBeforeDelete: function(dataObj, dataSources) {
		//custom code here


		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onBeforeDelete)
			return TenantSteamFlowMeterPointInterface.onBeforeDelete(dataObj, dataSources);

		return dataObj;
	},

	onAfterDelete: function(isSuccessful, resultObj, dataSources) {
		//Custom code here
		if(resultObj && resultObj.data && resultObj.data.err_code){
			resultObj = resultObj.data;
		}


		if (resultObj.err_code && resultObj.err_code === 422) {

			if (resultObj.refFlag === true) {

				let alertText = '<p>' + Strings.SFMP_FOREIGN_REFERENCE_EXIST;
				let refLabels = [];

				if (resultObj.conflictSources) {

					if (resultObj.conflictSources.connectivity_status)
						refLabels.push(Strings.SFMP_REF_CONNECTIVITY_STATUS);

					if (resultObj.conflictSources.sfm_link)
						refLabels.push(Strings.SFMP_REF_SFM_LINK);

					if (resultObj.conflictSources.utility)
						refLabels.push(Strings.SFMP_REF_UTILITY_DATA);
				}

				if (refLabels.length) {
					alertText += ' ' + refLabels.join(', ');
				}

				alertText += '</p>';
				alertText += '<ol class="sfmp_prompt_error">';
				alertText += '</ol>';

				s_info(Strings.VALIDATION_ERROR_TITLE, alertText, dlgOptions);
				clCommon.hideLoader("removeProgress");
			}


			return false;
		}


		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onAfterDelete)
			return TenantSteamFlowMeterPointInterface.onAfterDelete(isSuccessful, resultObj, dataSources);

		return resultObj;
	},

	onChangeFilter: function(changedFilter) {
		//Custom code here

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onChangeFilter)
			return TenantSteamFlowMeterPointInterface.onChangeFilter(changedFilter);

		return true;
	},

	onSearchChangeFilter: function(searchedFilter) {

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onSearchChangeFilter)
			return TenantSteamFlowMeterPointInterface.onSearchChangeFilter(searchedFilter);

		return true;
	},

	onRowItemClick: function(rowData) {
		//Custom code here

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onRowItemClick)
			return TenantSteamFlowMeterPointInterface.onRowItemClick(rowData);

		return rowData;
	},

	onDisplayForm: function(rowData) {
		//Custom code here

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onDisplayForm)
			return TenantSteamFlowMeterPointInterface.onDisplayForm(rowData);

		return rowData;
	},

	onClearForm: function(defaultObj, dataSources) {
		//Custom code here
		defaultObj.sfmp_active = true;

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onClearForm)
			return TenantSteamFlowMeterPointInterface.onClearForm(defaultObj, dataSources);

		return defaultObj;
	},

	onFieldFocus: function(focusedField, dataSources) {

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onFieldFocus)
			return TenantSteamFlowMeterPointInterface.onFieldFocus(focusedField, dataSources);

	},


	onFieldChange: function(changedField, dataSources) {

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onFieldChange)
			return TenantSteamFlowMeterPointInterface.onFieldChange(changedField, dataSources);

	},

	onToolBarBtnClick: function(changedField, dataSources) {

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onToolBarBtnClick)
			return TenantSteamFlowMeterPointInterface.onToolBarBtnClick(changedField, dataSources);

	},

	onLookupAddBtnClick: function(attribute, changedField, dataSources) {

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onLookupAddBtnClick)
			return TenantSteamFlowMeterPointInterface.onLookupAddBtnClick(attribute, changedField, dataSources);

	},

	onLookUpData: function(attribute, dataSources) {

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onLookUpData)
			return TenantSteamFlowMeterPointInterface.onLookUpData(attribute, dataSources);

		return true;

	},

	onChangeOfActionSelect: function(changedAction, dataSources) {

		switch (changedAction.value) {}

		if (TenantSteamFlowMeterPointInterface && TenantSteamFlowMeterPointInterface.onChangeOfActionSelect)
			return TenantSteamFlowMeterPointInterface.onChangeOfActionSelect(changedAction, dataSources);

		return true;
	}

};