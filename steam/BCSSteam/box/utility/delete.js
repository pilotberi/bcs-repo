/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Tue Jan 06 2026 10:03:38 GMT+0530 (India Standard Time)
	Description : Delete Service for utility
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var deleteUtilityInterface = require("deleteinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.DELETE_BCSSTEAM_UTILITY_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes;
  if (deleteUtilityInterface)
    onLoadRes = deleteUtilityInterface.onLoad(params);

  // Check if 'onLoad' override is true
  if (deleteUtilityInterface && onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (deleteUtilityInterface)
          respObj = deleteUtilityInterface.onError(respObj);
        console.error("Unexpected server error when deleting the utility - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes;
    if (deleteUtilityInterface)
      beforeDataRes = deleteUtilityInterface.beforeData(params);

    // Check if 'override' is true
    if (deleteUtilityInterface && beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          respObj = deleteUtilityInterface.onError(respObj);
        }
      }
    } else {
      if (deleteUtilityInterface && beforeDataRes.params)
        params = beforeDataRes.params;

      //Only if params is there
      if (params && params.id) {


        if (deleteUtilityInterface) {
          params = deleteUtilityInterface.beforeExecute(params);
        }
        //Sending request to ds client
        respObj = dsclientUtil.execute(params, "BCSSteam", "BCSSteam_utility", "delete");
      } else {
        respObj.code = 400;
        respObj.msg = Strings.DELETE_BCSSTEAM_UTILITY_ERROR;
      }
    }

    switch (respObj.code) {
      case 200:
        if (deleteUtilityInterface)
          respObj = deleteUtilityInterface.afterData(respObj);
        respObj.msg = Strings.DELETE_BCSSTEAM_UTILITY_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.DELETE_BCSSTEAM_UTILITY_ERROR;
        if (deleteUtilityInterface)
          respObj = deleteUtilityInterface.onError(respObj);
        console.error("Unable to delete the utility - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (deleteUtilityInterface)
          respObj = deleteUtilityInterface.onError(respObj);
        console.error("User access denied when deleting the utility - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (deleteUtilityInterface)
          respObj = deleteUtilityInterface.onError(respObj);
        console.error("Unexpected server error when deleting the utility - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (deleteUtilityInterface)
    errorRespObj = deleteUtilityInterface.onError(errorRespObj);
  console.error("Unexpected server error when deleting the utility - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}