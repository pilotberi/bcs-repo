/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Tue Mar 03 2026 12:10:08 GMT+0530 (India Standard Time)
	Description : Get All Service for connectivityOption
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var getAllConnectOptionInterface = require("getallinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.READ_BCSSTEAM_CONNECTIVITY_OPTION_GETALLCONNECTOPTION_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes = null;
  if (getAllConnectOptionInterface)
    onLoadRes = getAllConnectOptionInterface.onLoad(params);

  // Check if 'onLoad' override is true
  if (onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (getAllConnectOptionInterface)
          respObj = getAllConnectOptionInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the connectivityOption - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes = null;
    if (getAllConnectOptionInterface)
      beforeDataRes = getAllConnectOptionInterface.beforeData(params);

    // Check if 'override' is true
    if (beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          if (getAllConnectOptionInterface)
            respObj = getAllConnectOptionInterface.onError(respObj);
        }
      }
    } else {
      if (beforeDataRes && beforeDataRes.params)
        params = beforeDataRes.params;


      if (getAllConnectOptionInterface) {
        params = getAllConnectOptionInterface.beforeExecute(params);
      }

      var countRespObj = null;

      var getAllConnectOptionQueryParams = {};


      //Setting offset and limit after count query
      if (params.offset)
        getAllConnectOptionQueryParams.offset = params.offset;

      if (params.limit)
        getAllConnectOptionQueryParams.limit = params.limit;

      if (params.offset || params.limit)
        countRespObj = dsclientUtil.execute(getAllConnectOptionQueryParams, "BCSSteam", "BCSSteam_connectivity_option", "count");

      //Sending request to ds client
      respObj = dsclientUtil.execute(params, "BCSSteam", "BCSSteam_connectivity_option", "getAll");

      if (countRespObj)
        respObj.count = ((countRespObj.code == 200) && (countRespObj.data) && (countRespObj.data.count)) ? countRespObj.data.count : -1;
    }


    switch (respObj.code) {
      case 200:
        if (getAllConnectOptionInterface)
          respObj.data = getAllConnectOptionInterface.afterData(respObj.data);
        respObj.msg = Strings.READ_BCSSTEAM_CONNECTIVITY_OPTIONS_GETALLCONNECTOPTION_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.READ_BCSSTEAM_CONNECTIVITY_OPTIONS_GETALLCONNECTOPTION_ERROR;
        if (getAllConnectOptionInterface)
          respObj = getAllConnectOptionInterface.onError(respObj);
        console.error("Unable to retrieve the connectivityOption - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (getAllConnectOptionInterface)
          respObj = getAllConnectOptionInterface.onError(respObj);
        console.error("User access denied when retrieving the connectivityOption - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (getAllConnectOptionInterface)
          respObj = getAllConnectOptionInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the connectivityOption - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (getAllConnectOptionInterface)
    errorRespObj = getAllConnectOptionInterface.onError(errorRespObj);
  console.error("Unexpected server error when retrieving the connectivityOption - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}