/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Mon Mar 09 2026 10:55:15 GMT+0530 (India Standard Time)
	Description : Delete Service for sfmStatus
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var deleteSfmStatusInterface = require("deleteinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.DELETE_BCSSTEAM_SFM_STATUS_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes;
  if (deleteSfmStatusInterface)
    onLoadRes = deleteSfmStatusInterface.onLoad(params);

  // Check if 'onLoad' override is true
  if (deleteSfmStatusInterface && onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (deleteSfmStatusInterface)
          respObj = deleteSfmStatusInterface.onError(respObj);
        console.error("Unexpected server error when deleting the sfmStatus - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes;
    if (deleteSfmStatusInterface)
      beforeDataRes = deleteSfmStatusInterface.beforeData(params);

    // Check if 'override' is true
    if (deleteSfmStatusInterface && beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          respObj = deleteSfmStatusInterface.onError(respObj);
        }
      }
    } else {
      if (deleteSfmStatusInterface && beforeDataRes.params)
        params = beforeDataRes.params;

      //Only if params is there
      if (params && params.id) {


        if (deleteSfmStatusInterface) {
          params = deleteSfmStatusInterface.beforeExecute(params);
        }
        //Sending request to ds client
        respObj = dsclientUtil.execute(params, "BCSSteam", "BCSSteam_sfm_status", "delete");
      } else {
        respObj.code = 400;
        respObj.msg = Strings.DELETE_BCSSTEAM_SFM_STATUS_ERROR;
      }
    }

    switch (respObj.code) {
      case 200:
        if (deleteSfmStatusInterface)
          respObj = deleteSfmStatusInterface.afterData(respObj);
        respObj.msg = Strings.DELETE_BCSSTEAM_SFM_STATUS_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.DELETE_BCSSTEAM_SFM_STATUS_ERROR;
        if (deleteSfmStatusInterface)
          respObj = deleteSfmStatusInterface.onError(respObj);
        console.error("Unable to delete the sfmStatus - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (deleteSfmStatusInterface)
          respObj = deleteSfmStatusInterface.onError(respObj);
        console.error("User access denied when deleting the sfmStatus - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (deleteSfmStatusInterface)
          respObj = deleteSfmStatusInterface.onError(respObj);
        console.error("Unexpected server error when deleting the sfmStatus - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (deleteSfmStatusInterface)
    errorRespObj = deleteSfmStatusInterface.onError(errorRespObj);
  console.error("Unexpected server error when deleting the sfmStatus - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}