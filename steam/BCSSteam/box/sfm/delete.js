/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Mon Jan 05 2026 15:54:16 GMT+0530 (India Standard Time)
	Description : Delete Service for sfm
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var deleteSfmInterface = require("deleteinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.DELETE_BCSSTEAM_SFM_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes;
  if (deleteSfmInterface)
    onLoadRes = deleteSfmInterface.onLoad(params);

  // Check if 'onLoad' override is true
  if (deleteSfmInterface && onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (deleteSfmInterface)
          respObj = deleteSfmInterface.onError(respObj);
        console.error("Unexpected server error when deleting the sfm - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes;
    if (deleteSfmInterface)
      beforeDataRes = deleteSfmInterface.beforeData(params);

    // Check if 'override' is true
    if (deleteSfmInterface && beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          respObj = deleteSfmInterface.onError(respObj);
        }
      }
    } else {
      if (deleteSfmInterface && beforeDataRes.params)
        params = beforeDataRes.params;

      //Only if params is there
      if (params && params.id) {


        if (deleteSfmInterface) {
          params = deleteSfmInterface.beforeExecute(params);
        }
        //Sending request to ds client
        respObj = dsclientUtil.execute(params, "BCSSteam", "BCSSteam_sfm", "delete");
      } else {
        respObj.code = 400;
        respObj.msg = Strings.DELETE_BCSSTEAM_SFM_ERROR;
      }
    }

    switch (respObj.code) {
      case 200:
        if (deleteSfmInterface)
          respObj = deleteSfmInterface.afterData(respObj);
        respObj.msg = Strings.DELETE_BCSSTEAM_SFM_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.DELETE_BCSSTEAM_SFM_ERROR;
        if (deleteSfmInterface)
          respObj = deleteSfmInterface.onError(respObj);
        console.error("Unable to delete the sfm - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (deleteSfmInterface)
          respObj = deleteSfmInterface.onError(respObj);
        console.error("User access denied when deleting the sfm - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (deleteSfmInterface)
          respObj = deleteSfmInterface.onError(respObj);
        console.error("Unexpected server error when deleting the sfm - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (deleteSfmInterface)
    errorRespObj = deleteSfmInterface.onError(errorRespObj);
  console.error("Unexpected server error when deleting the sfm - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}