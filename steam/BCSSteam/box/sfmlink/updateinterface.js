/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Mon Mar 09 2026 13:33:44 GMT+0530 (India Standard Time)
	Description : Update Service Interface for sfmLink
	WARNING : DO NOT MODIFY THE GENERATED INTERFACE METHOD NAME OR ARGUMENTS
*/

//Use the following global objects as required.
//dsclientUtil - For data service related actions.
//Strings - For Strings dictionary. refer ../common/strings.js
//appcommon - For common help utils. refer ../common/common.js

var app = require('app');

var TenantUpdateSfmLinkInterface = app.getTenantWSInterface('BCSSteam', 'sfmlink/update');

if (typeof TenantUpdateSfmLinkInterface === 'undefined')
	TenantUpdateSfmLinkInterface = {};

module.exports = {
	onLoad: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		if (TenantUpdateSfmLinkInterface && TenantUpdateSfmLinkInterface.onLoad) {
			rObj = TenantUpdateSfmLinkInterface.onLoad(params);
		}

		return rObj;
	},

	beforeData: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		var hasError;

		if (params && params.data){
			try{
				params.data = JSON.parse(params.data);
			}catch(e){
				params.data = params.data;
			}

			var getAllSfmLinkRespObj = dsclientUtil.execute({}, "BCSSteam", "BCSSteam_sfm_link", "getAll");

			if (getAllSfmLinkRespObj && getAllSfmLinkRespObj.code == 200) {

				if (getAllSfmLinkRespObj.data && getAllSfmLinkRespObj.data.length > 0) {

					var paramSfmpId = params.data.sfmp_code_id;
					var paramStartDate = params.data.sfmc_start_date;

					for (var idx = 0; idx < getAllSfmLinkRespObj.data.length; idx++) {
						var record = getAllSfmLinkRespObj.data[idx];

						if (record.sfmp_code_id === paramSfmpId  && (params.data._sid != record._sid) ) {

							var recStartDate = record.sfmc_start_date;
							var recEndDate;

							if (record.sfmc_end_date) {
								recEndDate = record.sfmc_end_date;
							} else {
								recEndDate = null;
							}

							hasError = false;

							if (recStartDate < paramStartDate) {
								if (!recEndDate || recEndDate >= paramStartDate) {
									hasError = true; 
								}
							} else {
								hasError = true;
							}

							if (hasError) {
								rObj.override = true;
								rObj.resp = {
									"code" : 400,
									"msg" : "Date range conflicts",
									"err_code" : 422
								};
								break; 
							}
						}
					}
				}
			} else {
				rObj.override = true;
				rObj.resp = {
					"code" : 400,
					"msg" : Strings.ERROR || "Error fetching existing records"
				};
			}

			params.data = JSON.stringify(params.data);
		}



		if (TenantUpdateSfmLinkInterface && TenantUpdateSfmLinkInterface.beforeData) {
			rObj = TenantUpdateSfmLinkInterface.beforeData(params);
		}

		return rObj;
	},

	beforeExecute: function(queryParams) {
		//custom code here

		if (TenantUpdateSfmLinkInterface && TenantUpdateSfmLinkInterface.beforeExecute) {
			queryParams = TenantUpdateSfmLinkInterface.beforeExecute(queryParams);
		}
		return queryParams;
	},

	afterData: function(updateSfmLinkData) {


		if (TenantUpdateSfmLinkInterface && TenantUpdateSfmLinkInterface.afterData) {
			updateSfmLinkData = TenantUpdateSfmLinkInterface.afterData(updateSfmLinkData);
		}
		return updateSfmLinkData;
	},

	onError: function(errorObj) {

		if (TenantUpdateSfmLinkInterface && TenantUpdateSfmLinkInterface.onError) {
			errorObj = TenantUpdateSfmLinkInterface.onError(errorObj);
		}
		return errorObj;
	}
};