/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Mon Mar 09 2026 13:18:30 GMT+0530 (India Standard Time)
	Description : Get All Service for sfmLink
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var getAllSfmLinkInterface = require("getallinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.READ_BCSSTEAM_SFM_LINK_GETALLSFMLINK_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes = null;
  if (getAllSfmLinkInterface)
    onLoadRes = getAllSfmLinkInterface.onLoad(params);

  // Check if 'onLoad' override is true
  if (onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (getAllSfmLinkInterface)
          respObj = getAllSfmLinkInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the sfmLink - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes = null;
    if (getAllSfmLinkInterface)
      beforeDataRes = getAllSfmLinkInterface.beforeData(params);

    // Check if 'override' is true
    if (beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          if (getAllSfmLinkInterface)
            respObj = getAllSfmLinkInterface.onError(respObj);
        }
      }
    } else {
      if (beforeDataRes && beforeDataRes.params)
        params = beforeDataRes.params;


      if (getAllSfmLinkInterface) {
        params = getAllSfmLinkInterface.beforeExecute(params);
      }

      var countRespObj = null;

      var getAllSfmLinkQueryParams = {};


      //Setting offset and limit after count query
      if (params.offset)
        getAllSfmLinkQueryParams.offset = params.offset;

      if (params.limit)
        getAllSfmLinkQueryParams.limit = params.limit;

      if (params.offset || params.limit)
        countRespObj = dsclientUtil.execute(getAllSfmLinkQueryParams, "BCSSteam", "BCSSteam_sfm_link", "count");

      //Sending request to ds client
      respObj = dsclientUtil.execute(params, "BCSSteam", "BCSSteam_sfm_link", "getAll");

      if (countRespObj)
        respObj.count = ((countRespObj.code == 200) && (countRespObj.data) && (countRespObj.data.count)) ? countRespObj.data.count : -1;
    }


    switch (respObj.code) {
      case 200:
        if (getAllSfmLinkInterface)
          respObj.data = getAllSfmLinkInterface.afterData(respObj.data);
        respObj.msg = Strings.READ_BCSSTEAM_SFM_LINKS_GETALLSFMLINK_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.READ_BCSSTEAM_SFM_LINKS_GETALLSFMLINK_ERROR;
        if (getAllSfmLinkInterface)
          respObj = getAllSfmLinkInterface.onError(respObj);
        console.error("Unable to retrieve the sfmLink - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (getAllSfmLinkInterface)
          respObj = getAllSfmLinkInterface.onError(respObj);
        console.error("User access denied when retrieving the sfmLink - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (getAllSfmLinkInterface)
          respObj = getAllSfmLinkInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the sfmLink - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (getAllSfmLinkInterface)
    errorRespObj = getAllSfmLinkInterface.onError(errorRespObj);
  console.error("Unexpected server error when retrieving the sfmLink - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}