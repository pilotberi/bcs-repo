/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Fri Mar 06 2026 12:20:20 GMT+0530 (India Standard Time)
	Description : Create Service Interface for connectivityStatus
	WARNING : DO NOT MODIFY THE GENERATED INTERFACE METHOD NAME OR ARGUMENTS
*/

//Use the following global objects as required.
//dsclientUtil - For data service related actions.
//Strings - For Strings dictionary. refer ../common/strings.js
//appcommon - For common help utils. refer ../common/common.js

var app = require('app');

var TenantCreateConnectivityStatusInterface = app.getTenantWSInterface('BCSSteam', 'connectivitystatus/create');

if (typeof TenantCreateConnectivityStatusInterface === 'undefined')
	TenantCreateConnectivityStatusInterface = {};


module.exports = {
	onLoad: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};


		if (TenantCreateConnectivityStatusInterface && TenantCreateConnectivityStatusInterface.onLoad) {
			rObj = TenantCreateConnectivityStatusInterface.onLoad(params);
		}
		return rObj;
	},

	beforeData: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		var hasError;

		if (params && params.data){
			try{
				params.data = JSON.parse(params.data);
			}catch(e){
				params.data = params.data;
			}

			var getAllSfmpConnectStatusRespObj = dsclientUtil.execute({}, "BCSSteam", "BCSSteam_connectivity_status", "getAll");

			if (getAllSfmpConnectStatusRespObj && getAllSfmpConnectStatusRespObj.code == 200) {

				if (getAllSfmpConnectStatusRespObj.data && getAllSfmpConnectStatusRespObj.data.length > 0) {

					var paramSfmpId = params.data.sfmp_code_id;
					var paramStartDate = params.data.sfmc_start_date;

					for (var idx = 0; idx < getAllSfmpConnectStatusRespObj.data.length; idx++) {
						var record = getAllSfmpConnectStatusRespObj.data[idx];

						if (record.sfmp_code_id === paramSfmpId) {

							var recStartDate = record.sfmc_start_date;
							var recEndDate;

							if (record.sfmc_end_date) {
								recEndDate = record.sfmc_end_date;
							} else {
								recEndDate = null;
							}

							hasError = false;

							if (recStartDate < paramStartDate) {
								if (!recEndDate || recEndDate >= paramStartDate) {
									hasError = true; 
								}
							} else {
								hasError = true;
							}

							if (hasError) {
								rObj.override = true;
								rObj.resp = {
									"code" : 400,
									"msg" : "Date range conflicts",
									"err_code" : 422
								};
								break; 
							}
						}
					}
				}
			} else {
				rObj.override = true;
				rObj.resp = {
					"code" : 400,
					"msg" : Strings.ERROR || "Error fetching existing records"
				};
			}

			params.data = JSON.stringify(params.data);
		}


		if (TenantCreateConnectivityStatusInterface && TenantCreateConnectivityStatusInterface.beforeData) {
			rObj = TenantCreateConnectivityStatusInterface.beforeData(params);
		}
		return rObj;
	},

	beforeExecute: function(queryParams) {
		//custom code here

		if (TenantCreateConnectivityStatusInterface && TenantCreateConnectivityStatusInterface.beforeExecute) {
			queryParams = TenantCreateConnectivityStatusInterface.beforeExecute(queryParams);
		}
		return queryParams;
	},


	afterData: function(createConnectivityStatusData) {


		if (TenantCreateConnectivityStatusInterface && TenantCreateConnectivityStatusInterface.afterData) {
			createConnectivityStatusData = TenantCreateConnectivityStatusInterface.afterData(createConnectivityStatusData);
		}
		return createConnectivityStatusData;
	},

	onError: function(errorObj) {


		if (TenantCreateConnectivityStatusInterface && TenantCreateConnectivityStatusInterface.onError) {
			errorObj = TenantCreateConnectivityStatusInterface.onError(errorObj);
		}
		return errorObj;
	}
};