/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Fri Mar 06 2026 12:30:07 GMT+0530 (India Standard Time)
	Description : Get All Service for connectivityStatus
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var getAllConnectivityStatusInterface = require("getallinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.READ_BCSSTEAM_CONNECTIVITY_STATUS_GETALLCONNECTIVITYSTATUS_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes = null;
  if (getAllConnectivityStatusInterface)
    onLoadRes = getAllConnectivityStatusInterface.onLoad(params);

  // Check if 'onLoad' override is true
  if (onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (getAllConnectivityStatusInterface)
          respObj = getAllConnectivityStatusInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the connectivityStatus - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes = null;
    if (getAllConnectivityStatusInterface)
      beforeDataRes = getAllConnectivityStatusInterface.beforeData(params);

    // Check if 'override' is true
    if (beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          if (getAllConnectivityStatusInterface)
            respObj = getAllConnectivityStatusInterface.onError(respObj);
        }
      }
    } else {
      if (beforeDataRes && beforeDataRes.params)
        params = beforeDataRes.params;


      if (getAllConnectivityStatusInterface) {
        params = getAllConnectivityStatusInterface.beforeExecute(params);
      }

      var countRespObj = null;

      var getAllConnectivityStatusQueryParams = {};


      //Setting offset and limit after count query
      if (params.offset)
        getAllConnectivityStatusQueryParams.offset = params.offset;

      if (params.limit)
        getAllConnectivityStatusQueryParams.limit = params.limit;

      if (params.offset || params.limit)
        countRespObj = dsclientUtil.execute(getAllConnectivityStatusQueryParams, "BCSSteam", "BCSSteam_connectivity_status", "count");

      //Sending request to ds client
      respObj = dsclientUtil.execute(params, "BCSSteam", "BCSSteam_connectivity_status", "getAll");

      if (countRespObj)
        respObj.count = ((countRespObj.code == 200) && (countRespObj.data) && (countRespObj.data.count)) ? countRespObj.data.count : -1;
    }


    switch (respObj.code) {
      case 200:
        if (getAllConnectivityStatusInterface)
          respObj.data = getAllConnectivityStatusInterface.afterData(respObj.data);
        respObj.msg = Strings.READ_BCSSTEAM_CONNECTIVITY_STATUSES_GETALLCONNECTIVITYSTATUS_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.READ_BCSSTEAM_CONNECTIVITY_STATUSES_GETALLCONNECTIVITYSTATUS_ERROR;
        if (getAllConnectivityStatusInterface)
          respObj = getAllConnectivityStatusInterface.onError(respObj);
        console.error("Unable to retrieve the connectivityStatus - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (getAllConnectivityStatusInterface)
          respObj = getAllConnectivityStatusInterface.onError(respObj);
        console.error("User access denied when retrieving the connectivityStatus - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (getAllConnectivityStatusInterface)
          respObj = getAllConnectivityStatusInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the connectivityStatus - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (getAllConnectivityStatusInterface)
    errorRespObj = getAllConnectivityStatusInterface.onError(errorRespObj);
  console.error("Unexpected server error when retrieving the connectivityStatus - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}