/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Tue Mar 03 2026 11:59:01 GMT+0530 (India Standard Time)
	Description : Update Service Interface for sfmp
	WARNING : DO NOT MODIFY THE GENERATED INTERFACE METHOD NAME OR ARGUMENTS
*/

//Use the following global objects as required.
//dsclientUtil - For data service related actions.
//Strings - For Strings dictionary. refer ../common/strings.js
//appcommon - For common help utils. refer ../common/common.js

var app = require('app');

var TenantUpdateSfmpInterface = app.getTenantWSInterface('BCSSteam', 'sfmp/update');

if (typeof TenantUpdateSfmpInterface === 'undefined')
	TenantUpdateSfmpInterface = {};

module.exports = {
	onLoad: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		if (TenantUpdateSfmpInterface && TenantUpdateSfmpInterface.onLoad) {
			rObj = TenantUpdateSfmpInterface.onLoad(params);
		}

		return rObj;
	},

	beforeData: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		if (params && params.data){
			try{
				params.data = JSON.parse(params.data);
			}catch(e){
				params.data = params.data;
			}

			var getAllSfmpRespObj = dsclientUtil.execute({}, "BCSSteam", "BCSSteam_sfmp", "getAll");

			if(getAllSfmpRespObj.code == 200 && getAllSfmpRespObj.data){
				for(var idx = 0; idx < getAllSfmpRespObj.data.length; idx++){
					if(normalizeValue(params.data.sfmp_code) == normalizeValue(getAllSfmpRespObj.data[idx].sfmp_code)  && (params.data._sid != getAllSfmpRespObj.data[idx]._sid)){
						rObj.override = true;
						rObj.resp = {
							"code" : 400,
							"msg" : Strings.DUPLICATE_FOUND,
							"err_code" : 409 		//duplicate vendor
						};
					}
				}
			}else{
				rObj.override = true;
				rObj.resp = {
					"code" : 400,
					"msg" : Strings.CREATE_ERROR
				}
			}
		}

		params.data = JSON.stringify(params.data);


		if (TenantUpdateSfmpInterface && TenantUpdateSfmpInterface.beforeData) {
			rObj = TenantUpdateSfmpInterface.beforeData(params);
		}

		return rObj;
	},

	beforeExecute: function(queryParams) {
		//custom code here

		if (TenantUpdateSfmpInterface && TenantUpdateSfmpInterface.beforeExecute) {
			queryParams = TenantUpdateSfmpInterface.beforeExecute(queryParams);
		}
		return queryParams;
	},

	afterData: function(updateSfmpData) {


		if (TenantUpdateSfmpInterface && TenantUpdateSfmpInterface.afterData) {
			updateSfmpData = TenantUpdateSfmpInterface.afterData(updateSfmpData);
		}
		return updateSfmpData;
	},

	onError: function(errorObj) {

		if (TenantUpdateSfmpInterface && TenantUpdateSfmpInterface.onError) {
			errorObj = TenantUpdateSfmpInterface.onError(errorObj);
		}
		return errorObj;
	}
};


function normalizeValue(value) {
	if (!value) {
		return "";
	}

	return value.toString().toLowerCase().replace(/\s+/g, "");
}