/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Tue Jan 06 2026 10:01:49 GMT+0530 (India Standard Time)
	Description : Update Service Interface for utility
	WARNING : DO NOT MODIFY THE GENERATED INTERFACE METHOD NAME OR ARGUMENTS
*/

//Use the following global objects as required.
//dsclientUtil - For data service related actions.
//Strings - For Strings dictionary. refer ../common/strings.js
//appcommon - For common help utils. refer ../common/common.js

var app = require('app');

var TenantUpdateUtilityInterface = app.getTenantWSInterface('BCSSteam', 'utility/update');

if (typeof TenantUpdateUtilityInterface === 'undefined')
	TenantUpdateUtilityInterface = {};

module.exports = {
	onLoad: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		if (TenantUpdateUtilityInterface && TenantUpdateUtilityInterface.onLoad) {
			rObj = TenantUpdateUtilityInterface.onLoad(params);
		}

		return rObj;
	},

	beforeData: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		if (params && params.data){
			try{
				params.data = JSON.parse(params.data);
			}catch(e){
				params.data = params.data;
			}

			var getAllUtilityRespObj = dsclientUtil.execute({}, "BCSSteam", "BCSSteam_utility", "getAll");

			if(getAllUtilityRespObj.code == 200 && getAllUtilityRespObj.data){
				for(var idx = 0; idx < getAllUtilityRespObj.data.length; idx++){
					if(params.data.sfm_code_id == getAllUtilityRespObj.data[idx].sfm_code_id  &&
					   (params.data._sid != getAllUtilityRespObj.data[idx]._sid) &&
					   this._isSameMonth(params.data.record_date, getAllUtilityRespObj.data[idx].record_date)){

						rObj.override = true;
						rObj.resp = {
							"code" : 400,
							"msg" : Strings.DUPLICATE_FOUND,
							"err_code" : 409 		//duplicate vendor
						};
						break;
					}
				}
			}else{
				rObj.override = true;
				rObj.resp = {
					"code" : 400,
					"msg" : Strings.CREATE_BCSSTEAM_UTILITY_ERROR
				}
			}
		}

		params.data = JSON.stringify(params.data);

		if (TenantUpdateUtilityInterface && TenantUpdateUtilityInterface.beforeData) {
			rObj = TenantUpdateUtilityInterface.beforeData(params);
		}

		return rObj;
	},

	beforeExecute: function(queryParams) {
		//custom code here

		if (TenantUpdateUtilityInterface && TenantUpdateUtilityInterface.beforeExecute) {
			queryParams = TenantUpdateUtilityInterface.beforeExecute(queryParams);
		}
		return queryParams;
	},

	afterData: function(updateUtilityData) {


		if (TenantUpdateUtilityInterface && TenantUpdateUtilityInterface.afterData) {
			updateUtilityData = TenantUpdateUtilityInterface.afterData(updateUtilityData);
		}
		return updateUtilityData;
	},

	onError: function(errorObj) {

		if (TenantUpdateUtilityInterface && TenantUpdateUtilityInterface.onError) {
			errorObj = TenantUpdateUtilityInterface.onError(errorObj);
		}
		return errorObj;
	},


	_isSameMonth: function(creatingDate, existingDate) {
		const date1 = new Date(Number(creatingDate));
		const date2 = new Date(Number(existingDate));

		return (
			date1.getFullYear() === date2.getFullYear() &&
			date1.getMonth() === date2.getMonth()
		);
	}
};