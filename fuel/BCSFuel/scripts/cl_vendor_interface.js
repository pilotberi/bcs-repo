/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Fri Feb 13 2026 15:12:23 GMT+0530 (India Standard Time)
	Description : Interface Logic for BCSFuel List Details.
	WARNING : DO NOT MODIFY THE GENERATED INTERFACE METHOD NAME OR ARGUMENTS.
	Custom code can be writter here.
	Use customDtOpns object to maniuplate data table.
*/

if (typeof TenantVendorMapInterface === 'undefined')
	TenantVendorMapInterface = {};

var VendorMapInterface = {
	onLoad: function() {
		//Custom code goes here
		document.querySelector("title").innerHTML = "BCS Fuel";
		document.querySelector("span").innerHTML = "BCS Fuel";

		document.querySelector("button span[data-string-key='UPDATE_VENDORMAP']").innerHTML = "Update Vendor";
		document.querySelector("button span[data-string-key='CREATE_VENDORMAP']").innerHTML = "Create Vendor";

		clCommon.hide(document.getElementById("filterVendorMapBtn"));


		if (TenantVendorMapInterface && TenantVendorMapInterface.onLoad)
			return TenantVendorMapInterface.onLoad();

		return true;
	},

	onBeforeLoad: function(dataSources) {


		if (TenantVendorMapInterface && TenantVendorMapInterface.onBeforeLoad)
			return TenantVendorMapInterface.onBeforeLoad();

		return dataSources;
	},

	onAfterLoad: function(dataSources) {

		clCommon.hide(business_partner_idAddBtn);

		if (TenantVendorMapInterface && TenantVendorMapInterface.onAfterLoad)
			return TenantVendorMapInterface.onAfterLoad(dataSources);

		return dataSources;
	},

	onBeforeData: function(params, dataSources) {



		//Custom code goes here

		if (TenantVendorMapInterface && TenantVendorMapInterface.onBeforeData)
			return TenantVendorMapInterface.onBeforeData(params, dataSources);

		return true;
	},

	onAfterData: function(isSuccessful, dataArr, dataSources) {
		//custom code goes here

		if (TenantVendorMapInterface && TenantVendorMapInterface.onAfterData)
			return TenantVendorMapInterface.onAfterData(isSuccessful, dataArr, dataSources);

		return dataArr;
	},

	renderData: function(value, type, dtObj, meta) {
		//custom code goes here
		var rVal;

		switch(meta.col){
			case 0:
				rVal = this._getVendorName(value);
				break;

			case 1:
				rVal = Strings.NA_STRING;
				if(value){
					rVal = value;
				}

				break;

			case 2:
				rVal = Strings.FALSE_STRING;

				if(dtObj.isactive){
					rVal = Strings.TRUE_STRING;
				}
				break;
		}


		if (TenantVendorMapInterface && TenantVendorMapInterface.renderData)
			return TenantVendorMapInterface.renderData(value, type, dtObj, meta);

		return rVal;
	},

	onBeforeCreate: function(dataObj, dataSources) {
		//custom code here
		if (dataObj && dataObj.business_partner_short_name) {
			var value = dataObj.business_partner_short_name;

			if (value.length > 15) {
				s_info(Strings.ERROR, Strings.INVALID_SHORT_NAME_LENGTH, dlgOptions);
				return false;
			}
		}


		if (TenantVendorMapInterface && TenantVendorMapInterface.onBeforeCreate)
			return TenantVendorMapInterface.onBeforeCreate(dataObj, dataSources);

		return dataObj;
	},

	onAfterCreate: function(isSuccessful, dataObj, dataSources) {
		//Custom code here
		if(dataObj.err_code === 409){
			clCommon.hideLoader("createProgress");
			clCommon.enable(createVendorMapBtn);
			s_info(Strings.ERROR, Strings.DUPLICATE_VENDOR, dlgOptions);

			return false;
		}

		if (TenantVendorMapInterface && TenantVendorMapInterface.onAfterCreate)
			return TenantVendorMapInterface.onAfterCreate(isSuccessful, dataObj, dataSources);

		return dataObj;
	},

	onBeforeUpdate: function(dataObj, dataSources) {
		//custom code here
		if (dataObj && dataObj.business_partner_short_name) {
			var value = dataObj.business_partner_short_name;

			if (value.length > 15) {
				s_info(Strings.ERROR, Strings.INVALID_SHORT_NAME_LENGTH, dlgOptions);
				return false;
			}
		}


		if (TenantVendorMapInterface && TenantVendorMapInterface.onBeforeUpdate)
			return TenantVendorMapInterface.onBeforeUpdate(dataObj, dataSources);

		return dataObj;
	},

	onAfterUpdate: function(isSuccessful, dataObj, dataSources) {
		//Custom code here

		if(dataObj.err_code === 409){
			clCommon.hideLoader("updateProgress");
			clCommon.enable(editVendorMapBtn);
			s_info(Strings.ERROR, Strings.DUPLICATE_VENDOR, dlgOptions);

			return false;
		}


		if (TenantVendorMapInterface && TenantVendorMapInterface.onAfterUpdate)
			return TenantVendorMapInterface.onAfterUpdate(isSuccessful, dataObj, dataSources);

		return dataObj;
	},

	onBeforeDelete: function(dataObj, dataSources) {
		//custom code here


		if (TenantVendorMapInterface && TenantVendorMapInterface.onBeforeDelete)
			return TenantVendorMapInterface.onBeforeDelete(dataObj, dataSources);

		return dataObj;
	},

	onAfterDelete: function(isSuccessful, resultObj, dataSources) {
		//Custom code here
		if(resultObj.data && resultObj.data.err_code === 422){
			clCommon.hideLoader("removeProgress");
			s_info(Strings.ERROR, Strings.FOREIGN_REFERENCE_EXIST_ON_VENDOR, dlgOptions);

			return false;
		}


		if (TenantVendorMapInterface && TenantVendorMapInterface.onAfterDelete)
			return TenantVendorMapInterface.onAfterDelete(isSuccessful, resultObj, dataSources);

		return resultObj;
	},

	onChangeFilter: function(changedFilter) {
		//Custom code here

		if (TenantVendorMapInterface && TenantVendorMapInterface.onChangeFilter)
			return TenantVendorMapInterface.onChangeFilter(changedFilter);

		return true;
	},

	onSearchChangeFilter: function(searchedFilter) {

		if (TenantVendorMapInterface && TenantVendorMapInterface.onSearchChangeFilter)
			return TenantVendorMapInterface.onSearchChangeFilter(searchedFilter);

		return true;
	},

	onRowItemClick: function(rowData) {
		//Custom code here

		if(dataSources.getAllBusinessPartner){
			for (var bpIdx = 0; bpIdx < dataSources.getAllBusinessPartner.length; bpIdx++) {
				if (rowData.business_partner_id === dataSources.getAllBusinessPartner[bpIdx]._sid) {
					rowData.business_partner_name = dataSources.getAllBusinessPartner[bpIdx].name;
				}
			}
		}

		if (TenantVendorMapInterface && TenantVendorMapInterface.onRowItemClick)
			return TenantVendorMapInterface.onRowItemClick(rowData);

		return rowData;
	},

	onDisplayForm: function(rowData) {
		//Custom code here	

		if (TenantVendorMapInterface && TenantVendorMapInterface.onDisplayForm)
			return TenantVendorMapInterface.onDisplayForm(rowData);

		return rowData;
	},

	onClearForm: function(defaultObj, dataSources) {
		//Custom code here
		defaultObj.isactive = true;

		if (TenantVendorMapInterface && TenantVendorMapInterface.onClearForm)
			return TenantVendorMapInterface.onClearForm(defaultObj, dataSources);

		return defaultObj;
	},

	onFieldFocus: function(focusedField, dataSources) {

		if (TenantVendorMapInterface && TenantVendorMapInterface.onFieldFocus)
			return TenantVendorMapInterface.onFieldFocus(focusedField, dataSources);

	},


	onFieldChange: function(changedField, dataSources) {
		if (changedField.id === "vendorMapBusiness_partner_id") {
			var selectedCodeId = vendorMapBusiness_partner_id.getAttribute("business_partner_id");
			if(selectedCodeId && !vendorMapBusiness_partner_short_name.value){
				vendorMapBusiness_partner_short_name.value = vendorMapBusiness_partner_id.value;
			}
		}

		if (TenantVendorMapInterface && TenantVendorMapInterface.onFieldChange)
			return TenantVendorMapInterface.onFieldChange(changedField, dataSources);

	},

	onToolBarBtnClick: function(changedField, dataSources) {

		if (TenantVendorMapInterface && TenantVendorMapInterface.onToolBarBtnClick)
			return TenantVendorMapInterface.onToolBarBtnClick(changedField, dataSources);

	},

	onLookupAddBtnClick: function(attribute, changedField, dataSources) {

		if (TenantVendorMapInterface && TenantVendorMapInterface.onLookupAddBtnClick)
			return TenantVendorMapInterface.onLookupAddBtnClick(attribute, changedField, dataSources);

	},

	onLookUpData: function(attribute, dataSources) {

		if (TenantVendorMapInterface && TenantVendorMapInterface.onLookUpData)
			return TenantVendorMapInterface.onLookUpData(attribute, dataSources);

		return true;

	},

	onChangeOfActionSelect: function(changedAction, dataSources) {

		switch (changedAction.value) {}

		if (TenantVendorMapInterface && TenantVendorMapInterface.onChangeOfActionSelect)
			return TenantVendorMapInterface.onChangeOfActionSelect(changedAction, dataSources);

		return true;
	},

	_getVendorName : function(value){
		for(var customerIdx = 0; customerIdx < dataSources.getAllBusinessPartner.length; customerIdx++){
			var customerObj = dataSources.getAllBusinessPartner[customerIdx];
			if(value == customerObj._sid){
				value = customerObj.name;
				rVal = value
				break;
			}else{
				rVal = Strings.NA_STRING;

			}
		}
		return rVal;
	},


};