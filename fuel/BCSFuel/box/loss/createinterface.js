/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Tue Jan 27 2026 15:14:14 GMT+0530 (India Standard Time)
	Description : Create Service Interface for loss
	WARNING : DO NOT MODIFY THE GENERATED INTERFACE METHOD NAME OR ARGUMENTS
*/

//Use the following global objects as required.
//dsclientUtil - For data service related actions.
//Strings - For Strings dictionary. refer ../common/strings.js
//appcommon - For common help utils. refer ../common/common.js

var app = require('app');

var TenantCreateLossDataInterface = app.getTenantWSInterface('BCSFuel', 'loss/create');

if (typeof TenantCreateLossDataInterface === 'undefined')
	TenantCreateLossDataInterface = {};


module.exports = {
	onLoad: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};


		if (TenantCreateLossDataInterface && TenantCreateLossDataInterface.onLoad) {
			rObj = TenantCreateLossDataInterface.onLoad(params);
		}
		return rObj;
	},

	beforeData: function(params) {
		var rObj = {
			"override": false,
			"params": params,
		};

		if (params && params.data){
			try{
				params.data = JSON.parse(params.data);
			}catch(e){
				params.data = params.data;
			}

			var getAllLossRespObj = dsclientUtil.execute({}, "BCSFuel", "bcsfuel_loss", "getAll");

			if(getAllLossRespObj.code == 200 && getAllLossRespObj.data){
				for(var idx = 0; idx < getAllLossRespObj.data.length; idx++){
					if(params.data.fuel_code_id == getAllLossRespObj.data[idx].fuel_code_id  &&
					   this._isSameMonth(params.data.record_date, getAllLossRespObj.data[idx].record_date)){

						rObj.override = true;
						rObj.resp = {
							"code" : 400,
							"msg" : Strings.DUPLICATE_FOUND,
							"err_code" : 409 		//duplicate purchase
						};
						break;
					}
				}
			}else{
				rObj.override = true;
				rObj.resp = {
					"code" : 400,
					"msg" : Strings.CREATE_BCSFUEL_LOSS_ERROR
				}
			}
		}

		params.data = JSON.stringify(params.data);


		if (TenantCreateLossDataInterface && TenantCreateLossDataInterface.beforeData) {
			rObj = TenantCreateLossDataInterface.beforeData(params);
		}
		return rObj;
	},

	beforeExecute: function(queryParams) {
		//custom code here

		if (TenantCreateLossDataInterface && TenantCreateLossDataInterface.beforeExecute) {
			queryParams = TenantCreateLossDataInterface.beforeExecute(queryParams);
		}
		return queryParams;
	},


	afterData: function(createLossDataData) {


		if (TenantCreateLossDataInterface && TenantCreateLossDataInterface.afterData) {
			createLossDataData = TenantCreateLossDataInterface.afterData(createLossDataData);
		}
		return createLossDataData;
	},

	onError: function(errorObj) {


		if (TenantCreateLossDataInterface && TenantCreateLossDataInterface.onError) {
			errorObj = TenantCreateLossDataInterface.onError(errorObj);
		}
		return errorObj;
	},

	_isSameMonth: function (creatingDate, existingDate) {
		var date1 = new Date(Number(creatingDate));
		var date2 = new Date(Number(existingDate));

		return (
			date1.getFullYear() === date2.getFullYear() &&
			date1.getMonth() === date2.getMonth()
		);
	}
};