/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Tue Feb 03 2026 15:14:16 GMT+0530 (India Standard Time)
	Description : Query Service for loss
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var getLossByFilterInterface = require("getbyfilterinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.READ_BCSFUEL_LOSS_GETLOSSBYFILTER_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes;
  if (getLossByFilterInterface) {
    onLoadRes = getLossByFilterInterface.onLoad(params);
  }
  // Check if 'onLoad' override is true
  if (getLossByFilterInterface && onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (getLossByFilterInterface)
          respObj = getLossByFilterInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the loss - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes;
    if (getLossByFilterInterface) {
      beforeDataRes = getLossByFilterInterface.beforeData(params);
    }
    // Check if 'override' is true
    if (getLossByFilterInterface && beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          respObj = getLossByFilterInterface.onError(respObj);
        }
      }
    } else {
      if (getLossByFilterInterface && beforeDataRes.params)
        params = beforeDataRes.params;

      //Only if params is there
      if (params) {

        //Query Params
        var getLossByFilterQueryParams = {};

        getLossByFilterQueryParams.cols = ["record_date", "fuel_code_id", "fuel_unit_id", "fuel_yard_id", "fuel_loss_qty", "fuel_loss_percentage", "fuel_loss_value", "fuel_loss_gst_value", "fuel_loss_total_value", "unit_price", "remarks", "created_by", "created_on", "modified_by", "modified_on"];
        getLossByFilterQueryParams.filter = [];

        if (params.start_date) {
          getLossByFilterQueryParams.filter.push({
            "col": "record_date",
            "op": ">=",
            "val": parseInt(params.start_date)
          });
        }
        if (params.end_date) {
          getLossByFilterQueryParams.filter.push({
            "col": "record_date",
            "op": "<=",
            "val": parseInt(params.end_date)
          });
        }
        if (params.fuel_code_id && params.fuel_code_id != 'all') {
          getLossByFilterQueryParams.filter.push({
            "col": "fuel_code_id",
            "op": "=",
            "val": params.fuel_code_id
          });
        }








        if (getLossByFilterInterface) {
          getLossByFilterQueryParams = getLossByFilterInterface.beforeExecute(getLossByFilterQueryParams);
        }

        var countRespObj = null;

        if (params.offset || params.limit)
          countRespObj = dsclientUtil.execute(getLossByFilterQueryParams, "BCSFuel", "bcsfuel_loss", "count");

        //Setting offset and limit after count query
        if (params.offset)
          getLossByFilterQueryParams.offset = params.offset;

        if (params.limit)
          getLossByFilterQueryParams.limit = params.limit;

        //Sending request to ds client
        respObj = dsclientUtil.execute(getLossByFilterQueryParams, "BCSFuel", "bcsfuel_loss", "query");

        if (countRespObj)
          respObj.count = ((countRespObj.code == 200) && (countRespObj.data) && (countRespObj.data.count)) ? countRespObj.data.count : -1;

      } else {
        respObj.code = 400;
        respObj.msg = Strings.READ_BCSFUEL_LOSS_ERROR;
      }
    }


    switch (respObj.code) {
      case 200:
        if (getLossByFilterInterface)
          respObj.data = getLossByFilterInterface.afterData(respObj.data);
        respObj.msg = Strings.READ_BCSFUEL_LOSS_GETLOSSBYFILTER_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.READ_BCSFUEL_LOSS_GETLOSSBYFILTER_ERROR;
        if (getLossByFilterInterface)
          respObj = getLossByFilterInterface.onError(respObj);
        console.error("Unable to retrieve the loss - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (getLossByFilterInterface)
          respObj = getLossByFilterInterface.onError(respObj);
        console.error("User access denied when retrieving the loss - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (getLossByFilterInterface)
          respObj = getLossByFilterInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the loss - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (getLossByFilterInterface) {
    errorRespObj = getLossByFilterInterface.onError(errorRespObj);
  }
  console.error("Unexpected server error when retrieving the loss - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}