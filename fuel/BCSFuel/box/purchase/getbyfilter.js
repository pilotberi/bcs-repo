/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Sat Jan 31 2026 21:39:41 GMT+0530 (India Standard Time)
	Description : Query Service for purchase
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var getPurchaseByFilterInterface = require("getbyfilterinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.READ_BCSFUEL_PURCHASE_GETPURCHASEBYFILTER_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes;
  if (getPurchaseByFilterInterface) {
    onLoadRes = getPurchaseByFilterInterface.onLoad(params);
  }
  // Check if 'onLoad' override is true
  if (getPurchaseByFilterInterface && onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (getPurchaseByFilterInterface)
          respObj = getPurchaseByFilterInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the purchase - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes;
    if (getPurchaseByFilterInterface) {
      beforeDataRes = getPurchaseByFilterInterface.beforeData(params);
    }
    // Check if 'override' is true
    if (getPurchaseByFilterInterface && beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          respObj = getPurchaseByFilterInterface.onError(respObj);
        }
      }
    } else {
      if (getPurchaseByFilterInterface && beforeDataRes.params)
        params = beforeDataRes.params;

      //Only if params is there
      if (params) {

        //Query Params
        var getPurchaseByFilterQueryParams = {};

        getPurchaseByFilterQueryParams.cols = ["purchase_date", "fuel_code_id", "fuel_unit_id", "fuel_vendor_id", "fuel_qty", "fuel_value", "fuel_gst_value", "total_fuel_value", "unit_price", "remarks", "created_by", "created_on", "modified_by", "modified_on"];
        getPurchaseByFilterQueryParams.filter = [];

        if (params.start_date) {
          getPurchaseByFilterQueryParams.filter.push({
            "col": "purchase_date",
            "op": ">=",
            "val": parseInt(params.start_date)
          });
        }
        if (params.end_date) {
          getPurchaseByFilterQueryParams.filter.push({
            "col": "purchase_date",
            "op": "<=",
            "val": parseInt(params.end_date)
          });
        }
        if (params.fuel_code_id && params.fuel_code_id != 'all') {
          getPurchaseByFilterQueryParams.filter.push({
            "col": "fuel_code_id",
            "op": "=",
            "val": params.fuel_code_id
          });
        }








        if (getPurchaseByFilterInterface) {
          getPurchaseByFilterQueryParams = getPurchaseByFilterInterface.beforeExecute(getPurchaseByFilterQueryParams);
        }

        var countRespObj = null;

        if (params.offset || params.limit)
          countRespObj = dsclientUtil.execute(getPurchaseByFilterQueryParams, "BCSFuel", "bcsfuel_purchase", "count");

        //Setting offset and limit after count query
        if (params.offset)
          getPurchaseByFilterQueryParams.offset = params.offset;

        if (params.limit)
          getPurchaseByFilterQueryParams.limit = params.limit;

        //Sending request to ds client
        respObj = dsclientUtil.execute(getPurchaseByFilterQueryParams, "BCSFuel", "bcsfuel_purchase", "query");

        if (countRespObj)
          respObj.count = ((countRespObj.code == 200) && (countRespObj.data) && (countRespObj.data.count)) ? countRespObj.data.count : -1;

      } else {
        respObj.code = 400;
        respObj.msg = Strings.READ_BCSFUEL_PURCHASE_ERROR;
      }
    }


    switch (respObj.code) {
      case 200:
        if (getPurchaseByFilterInterface)
          respObj.data = getPurchaseByFilterInterface.afterData(respObj.data);
        respObj.msg = Strings.READ_BCSFUEL_PURCHASE_GETPURCHASEBYFILTER_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.READ_BCSFUEL_PURCHASE_GETPURCHASEBYFILTER_ERROR;
        if (getPurchaseByFilterInterface)
          respObj = getPurchaseByFilterInterface.onError(respObj);
        console.error("Unable to retrieve the purchase - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (getPurchaseByFilterInterface)
          respObj = getPurchaseByFilterInterface.onError(respObj);
        console.error("User access denied when retrieving the purchase - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (getPurchaseByFilterInterface)
          respObj = getPurchaseByFilterInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the purchase - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (getPurchaseByFilterInterface) {
    errorRespObj = getPurchaseByFilterInterface.onError(errorRespObj);
  }
  console.error("Unexpected server error when retrieving the purchase - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}