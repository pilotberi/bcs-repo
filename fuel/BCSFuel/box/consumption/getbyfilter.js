/*
	Generated By : Steps Code Generator.
	Version : 2024
	Generated On : Mon Feb 02 2026 18:13:04 GMT+0530 (India Standard Time)
	Description : Query Service for consumption
	WARNING : DO NOT MODIFY THIS FILE
*/

//External library
var dsclientUtil = require('dsclient');


//Internal library
var Strings = require("../common/strings.js");
var appcommon = require("../common/common.js");
var getConsumptionByFilterInterface = require("getbyfilterinterface.js");

try {

  //Setting cache as null
  $g.response.headers["Cache-Control"] = "max-age=0";
  $g.response.headers["Cache-Control"] = "no-store";


  //Success response object
  var respObj = {
    code: 200,
    msg: Strings.READ_BCSFUEL_CONSUMPTION_GETCONSUMPTIONBYFILTER_SUCCESS
  };


  //Checking whether the request is in query or body
  var params = appcommon.processParams($g.request);
  var paramStr = JSON.stringify(params);

  //check for Language setup 
  if (params && params.l) {
    var filePath = "/common/lang/";
    appcommon.initLanguage(params.l, filePath);
  }


  // Call 'onLoad' method from the Interface with params as the argument
  var onLoadRes;
  if (getConsumptionByFilterInterface) {
    onLoadRes = getConsumptionByFilterInterface.onLoad(params);
  }
  // Check if 'onLoad' override is true
  if (getConsumptionByFilterInterface && onLoadRes && onLoadRes.override === true) {
    if (onLoadRes.resp) {
      if (onLoadRes.resp.code) {
        respObj = onLoadRes.resp;
      } else {
        respObj.code = 500;
        respObj.msg = Strings.SERVER_ERROR;
        if (getConsumptionByFilterInterface)
          respObj = getConsumptionByFilterInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the consumption - " + paramStr);
      }
    }
  } else {

    // Calling 'beforeData' method from Interface and passing params as argument
    var beforeDataRes;
    if (getConsumptionByFilterInterface) {
      beforeDataRes = getConsumptionByFilterInterface.beforeData(params);
    }
    // Check if 'override' is true
    if (getConsumptionByFilterInterface && beforeDataRes && beforeDataRes.override === true) {
      //Sending request to ds client
      if (beforeDataRes.resp) {
        if (beforeDataRes.resp.code) {
          respObj = beforeDataRes.resp;
        } else {
          respObj.code = 500;
          respObj.msg = Strings.SERVER_ERROR;
          respObj = getConsumptionByFilterInterface.onError(respObj);
        }
      }
    } else {
      if (getConsumptionByFilterInterface && beforeDataRes.params)
        params = beforeDataRes.params;

      //Only if params is there
      if (params) {

        //Query Params
        var getConsumptionByFilterQueryParams = {};

        getConsumptionByFilterQueryParams.cols = ["record_date", "fuel_utilization_point_id", "fuel_code_id", "consumed_qty", "fuel_unit_id", "avg_consumption_per_day", "consumed_value", "consumed_gst_value", "consumed_total_value", "unit_price", "remarks", "created_by", "created_on", "modified_by", "modified_on"];
        getConsumptionByFilterQueryParams.filter = [];

        if (params.start_date) {
          getConsumptionByFilterQueryParams.filter.push({
            "col": "record_date",
            "op": ">=",
            "val": parseInt(params.start_date)
          });
        }
        if (params.end_date) {
          getConsumptionByFilterQueryParams.filter.push({
            "col": "record_date",
            "op": "<=",
            "val": parseInt(params.end_date)
          });
        }
        if (params.fuel_code_id && params.fuel_code_id != 'all') {
          getConsumptionByFilterQueryParams.filter.push({
            "col": "fuel_code_id",
            "op": "=",
            "val": params.fuel_code_id
          });
        }








        if (getConsumptionByFilterInterface) {
          getConsumptionByFilterQueryParams = getConsumptionByFilterInterface.beforeExecute(getConsumptionByFilterQueryParams);
        }

        var countRespObj = null;

        if (params.offset || params.limit)
          countRespObj = dsclientUtil.execute(getConsumptionByFilterQueryParams, "BCSFuel", "bcsfuel_consumption", "count");

        //Setting offset and limit after count query
        if (params.offset)
          getConsumptionByFilterQueryParams.offset = params.offset;

        if (params.limit)
          getConsumptionByFilterQueryParams.limit = params.limit;

        //Sending request to ds client
        respObj = dsclientUtil.execute(getConsumptionByFilterQueryParams, "BCSFuel", "bcsfuel_consumption", "query");

        if (countRespObj)
          respObj.count = ((countRespObj.code == 200) && (countRespObj.data) && (countRespObj.data.count)) ? countRespObj.data.count : -1;

      } else {
        respObj.code = 400;
        respObj.msg = Strings.READ_BCSFUEL_CONSUMPTION_ERROR;
      }
    }


    switch (respObj.code) {
      case 200:
        if (getConsumptionByFilterInterface)
          respObj.data = getConsumptionByFilterInterface.afterData(respObj.data);
        respObj.msg = Strings.READ_BCSFUEL_CONSUMPTION_GETCONSUMPTIONBYFILTER_SUCCESS;
        break;
      case 400:
        respObj.msg = Strings.READ_BCSFUEL_CONSUMPTION_GETCONSUMPTIONBYFILTER_ERROR;
        if (getConsumptionByFilterInterface)
          respObj = getConsumptionByFilterInterface.onError(respObj);
        console.error("Unable to retrieve the consumption - " + paramStr);
        break;
      case 403:
        respObj.msg = Strings.ACCESS_DENIED;
        if (getConsumptionByFilterInterface)
          respObj = getConsumptionByFilterInterface.onError(respObj);
        console.error("User access denied when retrieving the consumption - " + paramStr);
        break;
      case 500:
        respObj.msg = Strings.SERVER_ERROR;
        if (getConsumptionByFilterInterface)
          respObj = getConsumptionByFilterInterface.onError(respObj);
        console.error("Unexpected server error when retrieving the consumption - " + paramStr);
        break;
    }

  }


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = respObj.code;
  $g.response.body = JSON.stringify(respObj);

} catch (e) {

  //Error response object
  var errorRespObj = {
    status: 500,
    message: Strings.SERVER_ERROR + " - " + e.message,
    stack: e.stack
  };
  if (getConsumptionByFilterInterface) {
    errorRespObj = getConsumptionByFilterInterface.onError(errorRespObj);
  }
  console.error("Unexpected server error when retrieving the consumption - " + paramStr + "-" + errorRespObj.message);


  $g.response.headers["content-type"] = "application/json";
  $g.response.status = errorRespObj.status;
  $g.response.body = JSON.stringify(errorRespObj);

}